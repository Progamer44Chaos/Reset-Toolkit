# Reset-Tool
Batch scripts to reset core Windows components (WLAN, DNS, Firewall, Defender, Store, Printer, Updates).   Includes a menu (`Reset-Tool.bat`) for easy selection. Works on all Windows PCs with admin rights.
